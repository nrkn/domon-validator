{
  "id": "domon-childNodes",
  "title": "DOMON child nodes",
  "type": "array",
  "items": {
    "anyOf": [
      { "$ref": "domon-element" },
      { "$ref": "domon-text" },
      { "$ref": "domon-processingInstruction" },
      { "$ref": "domon-comment" },
      { "$ref": "domon-documentType" }
    ]
  }
}
